import { RuleHistoryDescriptionPipe } from './rule-history-description.pipe';

describe('RuleHistoryDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RuleHistoryDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
