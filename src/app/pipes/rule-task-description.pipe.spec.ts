import { RuleTaskDescriptionPipe } from './rule-task-description.pipe';

describe('RuleTaskDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new RuleTaskDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
